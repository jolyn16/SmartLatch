package com.example.smartlatch.model

class WelcomePageModel {
    // If you need data later (like preferences, API calls), put it here.
    fun isFirstTimeUser(): Boolean {
        return true // Stubbed example, can connect to SharedPreferences later
    }
}
